import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const start = () => {
  return (
    <ScrollView>
      <View>
        <Text>start</Text>
      </View>
    </ScrollView>
  )
}

export default start